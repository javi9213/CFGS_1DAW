import lista_circular.listac.CElemento;

public class colad {
	
	private class CElemento{
		private char dato;
		private CElemento siguiente;
		
		public CElemento(char d){
			this.dato=d;
			siguiente=null;
		}
	}

	private int numElem;
	private CElemento cola;
	
	public colad(){
		numElem = 0;
	}
	
	public int size(){
		return numElem;
	}
	
	public void push(char c){		//anadir
		CElemento ch = new CElemento(c);
		if(cola != null){
			ch.siguiente = cola.siguiente;
			cola.siguiente = ch;
		}else{
			cola = ch;
			cola.siguiente = ch;
		}
		cola = cola.siguiente;
		numElem++;
	}
	
	public char pop(){				//obtener
		CElemento q = cola.siguiente;
		char obj = q.dato;

		if (cola == null) {
			System.out.println("Lista vacia");
		} else {
			if (q == cola) {
				cola = null;
			} else {
				cola.siguiente = q.siguiente;
			}
		}
		numElem--;

		return obj;
	}
}